
<!-- ******************* LATEX: image + code display together ******************* -->

<!-- Read in the code -->
```{r LargerTwoNumbers, echo=FALSE, fig.show='hide', fig.width=6, fig.height=4, out.width='70%'}
my_Code <- readLines("LargerTwoNumbers.R")
eval(parse(text = my_Code))
plot_Path <- knitr::fig_chunk('LargerTwoNumbers', 
                              'pdf')
```


```{=latex}
\begin{figure}[htbp]
  \centering

  \begin{minipage}{0.9\linewidth}
    \begin{lstlisting}[language=R]
`r paste(my_Code, collapse = "\n")`
    \end{lstlisting}
  \end{minipage}

  \includegraphics[width=0.8\linewidth]{`r plot_Path`}
  \vspace{0.5em} % Small vertical space between plot and code
  \caption{The probability function for the larger of two numbers drawn.}
  \label{fig:ProbDist1} 
\end{figure}
```




<!-- ******************* HTML: image, then code display ******************* -->
```{r ProbDist1, echo=FALSE, fig.cap="The probability function for the larger of two numbers drawn.", out.width='100%', eval=knitr::is_html_output(), include=knitr::is_html_output()}
eval(parse(text = my_Code))
```


```{r echo=FALSE, results='asis', eval=knitr::is_html_output()}
code_html <- show_Code("LargerTwoNumbers.R")
code_html <- c('<details><summary>Show R code</summary>\n\n',
               code_html,
               '\n</details>\n')
cat(paste(code_html, collapse = "\n"))
```


