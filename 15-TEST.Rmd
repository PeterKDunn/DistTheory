

# Testing 

TESTING:




```{=html}
<details>
<summary>Show R code</summary>

<pre><code class="language-r">
plot( x = c(-1, 2),
      y = c(0, 2),
      type = "n",
      las = 1,
      main = expression(
        paste("The probability distribution of ", italic(W))
        ),
      xlab = expression(
        paste("Values of the rv ", italic(W))
        ),
      ylab = expression(
        paste("The probability function ", italic(p)[italic(W)](italic(w)))
      )
)

polygon( x = c(0, 0.5, 0.5, 0),
         y = c(0, 1, 0, 0),
         col = plotColour1)
</code></pre>
</details>
```


::: {.figure-with-code}

**FIRST R CHUNK (generates the plot, code hidden)**
```{r plot-example, echo=FALSE, fig.width=6, fig.asp=0.6, out.width='60%', fig.align='center', fig.cap="A scatter plot of cars data.", results='asis', comment=""}
show_Code({plot(cars, main = "Speed vs. Stopping Distance",
          xlab = "Speed (mph)", ylab = "Stopping Distance (ft)",
          col = "blue", pch = 16, cex = 1.2,
          lwd = 2, las = 1)})

# This R code generates the plot.
plot(cars, main = "Speed vs. Stopping Distance",
     xlab = "Speed (mph)", ylab = "Stopping Distance (ft)",
     col = "blue", pch = 16, cex = 1.2,
     lwd = 2, las = 1)
```

:::


